{
  "name": "job-bridge",
  "version": "0.0.1",
  "private": true,
  "scripts": {
  "start": "node server.js"
},
"main": "server.js",  
  "dependencies": {
    npm install --save "ejs": "^1.0.0",
    npm install --save "express": "3.5.1",
    npm install --save "express-myconnection": "1.0.4",
    npm install --save "jade": "*",
    npm install --save "mysql": "2.2.0",
    npm install --save "path": "^0.12.7",
    npm install --save "sequelize": "^3.25.0"
  }
}
